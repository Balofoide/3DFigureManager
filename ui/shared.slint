
export struct Database {
    nome: string,
    endereco: string,
    entrega: string,
    preco: float,
}

export struct Estoque_Database {
    material: string,
    quantidade: string,
    quantidade_total: int,
    medida: string,
}

export struct Impressoras {
    modelo: string,
    watts: string,
}
